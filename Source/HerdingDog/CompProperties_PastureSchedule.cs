using Verse;

namespace HerdingDog
{
    public class CompProperties_PastureSchedule : CompProperties
    {
        public CompProperties_PastureSchedule()
        {
            compClass = typeof(CompPastureSchedule);
        }
    }
}

